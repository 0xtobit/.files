[user]
	name = Tobit Raff
	email = 0xtobit@gmail.com
[credential]
	helper = cache
[color]
	ui = auto
[push]
	default = simple
[core]
	excludesfile = ~/.gitignore
	editor = /usr/bin/vim
[alias]
    wdiff = diff --word-diff
    sdiff = diff --cached
    swdiff = diff --cached --word-diff
    wsdiff = diff --cached --word-diff
    unstage = reset
    co = checkout
    rbdiff = diff --no-color --full-index --ignore-submodules --no-ext-diff
